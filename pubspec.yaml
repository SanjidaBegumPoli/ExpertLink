name: expertlink
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  provider: ^6.0.5

  # Firebase
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1

  google_sign_in: ^6.1.0
  http: ^1.2.0
  image_picker: ^1.1.1
  file_picker: ^10.3.3
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/aiMl.png
    - lib/assets/CP.png
    - lib/assets/UiUx.png
    - lib/assets/webDevelopment.png
    - lib/assets/app_development.png
    - lib/assets/cyberSecurity.png